import type { CollectionConfig } from 'payload'

export const Makes: CollectionConfig = {
  slug: 'makes',
  admin: {
    useAsTitle: 'name',
  },
  fields: [
    {
      name: 'name',
      type: 'text',
      required: true,
      localized: true,
    },
    {
      name: 'slug',
      type: 'text',
      required: true,
      unique: true,
      admin: {
        description: 'URL-safe identifier, e.g. "toyota"',
      },
    },
  ],
}
